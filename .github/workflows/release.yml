name: Release

# The single add-in release pipeline. Hourly (and on demand) it tracks the latest
# PUBLISHED oblikovati.org/api release and, when the bridge has not shipped for it yet:
#   1. regenerates the MCP tool surface from that API release,
#   2. builds the c-shared add-in for every OS,
#   3. publishes a GitHub release (tag `api-<version>`), and
#   4. commits the bumped go.mod pin + regenerated surface back to the default branch,
#      so the repo always reflects the API release the bridge is built against.
# It is idempotent: a run for an API release that already shipped is a no-op.
# Publishing the bundles to the add-in catalogue is a separate terminal job (`catalogue`)
# that never gates the release — see the comment on that job.
on:
  schedule:
    - cron: "0 * * * *" # top of every hour
  workflow_dispatch:
    inputs:
      api_ref:
        description: "API release to build against (tag/sha; default: the latest published release)"
        required: false
        default: ""

# Least privilege: permissions are granted per job (none at the workflow level), so only
# the publish/sync jobs can write.

# One release at a time (the sync job pushes to the default branch).
concurrency:
  group: release
  cancel-in-progress: false

defaults:
  run:
    shell: bash

jobs:
  # Resolve the API release to track and decide whether the bridge needs building.
  check:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      build: ${{ steps.decide.outputs.build }}
      api_tag: ${{ steps.decide.outputs.api_tag }}
      tag: ${{ steps.decide.outputs.tag }}
    steps:
      - id: decide
        env:
          GH_TOKEN: ${{ github.token }}
          OVERRIDE: ${{ github.event.inputs.api_ref }}
        run: |
          set -euo pipefail
          api_tag="$OVERRIDE"
          if [ -z "$api_tag" ]; then
            api_tag=$(gh release view -R Oblikovati/Oblikovati.API --json tagName -q .tagName)
          fi
          if [ -z "$api_tag" ]; then
            echo "no oblikovati.org/api release found" >&2
            exit 1
          fi
          tag="api-$api_tag" # the bridge release is named for the API release it tracks
          if gh release view "$tag" -R "$GITHUB_REPOSITORY" >/dev/null 2>&1; then
            echo "build=false" >> "$GITHUB_OUTPUT"
            echo "bridge already shipped for API $api_tag ($tag) — nothing to do."
          else
            echo "build=true" >> "$GITHUB_OUTPUT"
            echo "API released $api_tag — building bridge $tag."
          fi
          echo "api_tag=$api_tag" >> "$GITHUB_OUTPUT"
          echo "tag=$tag" >> "$GITHUB_OUTPUT"

  # Regenerate the tool surface from that API release and build the c-shared add-in per OS.
  build:
    needs: check
    if: needs.check.outputs.build == 'true'
    permissions:
      contents: read
    # One native runner per target platform, so the host can install the binary for its own
    # OS+arch (the catalogue stores a bundle per platform; #1164). c-shared is cgo, so each
    # platform must be built on its own native runner — no cross-compiling.
    strategy:
      fail-fast: false
      matrix:
        # darwin-amd64 is cross-built on the macos-14 (arm64) runner, so no Intel runner (#1164).
        os: [ubuntu-latest, ubuntu-24.04-arm, macos-14, windows-latest]
    runs-on: ${{ matrix.os }}
    env:
      CGO_ENABLED: "1"
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: false
      - uses: ./.github/actions/siblings
        with:
          api-ref: ${{ needs.check.outputs.api_tag }}
      - name: Regenerate tool surface from the API release
        run: go generate ./...
      - name: Build per-platform bundles
        # macOS builds BOTH arches — darwin-amd64 is cross-compiled on the arm64 runner (the
        # macOS toolchain is universal), so Intel Macs are covered without an Intel runner.
        shell: bash
        run: |
          set -euo pipefail
          case "$RUNNER_OS" in
            Windows) ext=dll;   targets="windows-amd64" ;;
            macOS)   ext=dylib; targets="darwin-arm64 darwin-amd64" ;;
            *)
              ext=so; a=$(uname -m)
              case "$a" in x86_64|amd64) a=amd64 ;; aarch64|arm64) a=arm64 ;; esac
              targets="linux-$a" ;;
          esac
          mkdir -p dist
          for plat in $targets; do
            GOARCH="${plat##*-}" go build -buildmode=c-shared -o "dist/oblikovati-mcp-bridge-$plat.$ext" .
          done
          cp manifest.json dist/manifest.json
      - uses: actions/upload-artifact@v6
        with:
          name: bridge-${{ matrix.os }}
          path: dist/*

  # Cut the bridge GitHub release for this API version.
  publish:
    needs: [check, build]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v7
        with:
          path: dist
          merge-multiple: true
      - name: Create release
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ needs.check.outputs.tag }}
          API_TAG: ${{ needs.check.outputs.api_tag }}
        run: |
          gh release create "$TAG" dist/* \
            -R "$GITHUB_REPOSITORY" \
            --title "MCP bridge — API $API_TAG" \
            --notes "Auto-built oblikovati-mcp-bridge against oblikovati.org/api $API_TAG. The MCP tool surface is generated from that release's mcp: annotations. Artifacts: c-shared library per OS + manifest.json."

  # Publish the built bundles to the add-in catalogue. This is a TERMINAL job on purpose:
  # nothing depends on it, so a catalogue outage, an unprovisioned token or a rejected bundle
  # cannot stop the GitHub release from shipping.
  #
  # It used to run inside `build`, where a 401 from the catalogue failed the build and so
  # blocked the release itself. Because `check` only short-circuits once the release EXISTS,
  # the hourly schedule then retried the same failure indefinitely — two add-ins sat broken
  # for a month that way. Cutting the release first ends that loop: the next run finds the
  # release and no-ops. A publish that fails here is retried on its own with "Re-run failed
  # jobs" on this run, without cutting a new release.
  catalogue:
    needs: [check, build, publish]
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v5
      - uses: actions/download-artifact@v7
        with:
          path: dist
          merge-multiple: true
      - name: Publish bundles to the catalogue
        env:
          ADDINS_PUBLISH_TOKEN: ${{ secrets.ADDINS_PUBLISH_TOKEN }}
          ADDINS_PUBLISH_URL: ${{ secrets.ADDINS_PUBLISH_URL }}
          API_TAG: ${{ needs.check.outputs.api_tag }}
        run: |
          set -euo pipefail
          if [ -z "${ADDINS_PUBLISH_TOKEN:-}" ]; then
            echo "::notice::ADDINS_PUBLISH_TOKEN is not set — skipping the catalogue publish."
            exit 0
          fi
          api="${API_TAG#v}"; api="${api%.*}"
          shopt -s nullglob
          # The c-shared build emits a .h beside each library, so match the library extensions
          # explicitly rather than globbing everything the build job uploaded.
          libs=(dist/oblikovati-mcp-bridge-*.so dist/oblikovati-mcp-bridge-*.dylib dist/oblikovati-mcp-bridge-*.dll)
          if [ ${#libs[@]} -eq 0 ]; then
            echo "no built libraries in dist/ — the build job uploaded nothing to publish" >&2
            exit 1
          fi
          for lib in "${libs[@]}"; do
            plat="${lib##*/oblikovati-mcp-bridge-}"
            scripts/publish-catalogue.sh "${plat%.*}" "$lib" "$api"
          done

  # Commit the bumped pin + regenerated surface back, so the repo matches what shipped.
  sync:
    needs: [check, build, publish]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: false
      - uses: ./.github/actions/siblings
        with:
          api-ref: ${{ needs.check.outputs.api_tag }}
      - name: Pin go.mod to the API release and regenerate
        env:
          API_TAG: ${{ needs.check.outputs.api_tag }}
        run: |
          set -euo pipefail
          go mod edit -require "oblikovati.org/api@$API_TAG"
          go generate ./...
          go mod tidy
          # The replaces are CI-only; never commit them.
          go mod edit -dropreplace oblikovati.org/api -dropreplace oblikovati.org
      - name: Commit if changed
        env:
          API_TAG: ${{ needs.check.outputs.api_tag }}
          BRANCH: ${{ github.ref_name }}
        run: |
          set -euo pipefail
          if git diff --quiet -- go.mod go.sum bridge/tools_generated.go; then
            echo "repo already tracks API $API_TAG — nothing to commit."
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add go.mod go.sum bridge/tools_generated.go
          git commit -m "chore: bridge tracks oblikovati.org/api $API_TAG [skip ci]"
          git push origin "HEAD:$BRANCH"
